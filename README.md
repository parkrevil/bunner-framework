# bunner-workspace

VS Code workspace meta-repository for Bunner multi-repo development.

This repository intentionally does not vendor the product repositories. Clone the repos listed in `bunner-framework.code-workspace` into this directory, then open the workspace file.
